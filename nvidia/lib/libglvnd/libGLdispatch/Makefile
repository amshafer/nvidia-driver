NVIDIA_ROOT=	${.CURDIR}/../../..
OBJDIR=obj/libglvnd

.if !defined(LOCALBASE)
LOCALBASE=	/usr/local
.endif

LIB=		GLdispatch
SHLIB_MAJOR=	0
LIBDIR=		${LOCALBASE}/lib

.include <${NVIDIA_ROOT}/mk/nvidia.lib.mk>
