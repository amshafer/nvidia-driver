NVIDIA_ROOT=	${.CURDIR}/../../..
OBJDIR=obj/libglvnd

.if !defined(LOCALBASE)
LOCALBASE=	/usr/local
.endif

LIB=		GL
SHLIB_MAJOR=	1
LIBDIR=		${LOCALBASE}/lib

.include <${NVIDIA_ROOT}/mk/nvidia.lib.mk>
